<Window x:Class="MiniBrain.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MiniBrain Desktop - Agentic AI Assistant" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">
    
    <Window.Resources>
        <!-- Modern Dark Theme -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF3F3F46"/>
        
        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1B9AE4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🧠" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock Text="MiniBrain Desktop" 
                             FontSize="20" FontWeight="Bold" 
                             Foreground="{StaticResource TextBrush}"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="Agentic AI Assistant" 
                             FontSize="12" 
                             Foreground="{StaticResource SubTextBrush}"
                             VerticalAlignment="Bottom" Margin="10,0,0,2"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔧 Settings" Style="{StaticResource ModernButton}" 
                            Margin="0,0,10,0" Click="Settings_Click"/>
                    <Button Content="ℹ️ About" Style="{StaticResource ModernButton}" 
                            Click="About_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Agents & Goals -->
            <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" 
                    CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Agents Section -->
                        <!-- NOTE: Agent functionality currently disabled - using direct Claude API calls -->
                        <TextBlock Text="🤖 AI Agents" FontSize="16" FontWeight="Bold" 
                                 Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                        
                        <!-- Agent creation disabled - direct Claude API mode -->
                        <Button Content="➕ Create New Agent" Style="{StaticResource ModernButton}"
                                HorizontalAlignment="Stretch" Margin="0,0,0,10"
                                Click="CreateAgent_Click" IsEnabled="False" ToolTip="Agent creation disabled - using direct Claude API"/>
                        
                        <ListBox x:Name="AgentsListBox" Background="Transparent" 
                               BorderThickness="0" Margin="0,0,0,20"
                               Foreground="{StaticResource TextBrush}"
                               SelectionChanged="AgentsListBox_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FF3F3F46"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                        
                        <!-- Goals Section -->
                        <TextBlock Text="🎯 Active Goals" FontSize="16" FontWeight="Bold" 
                                 Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
                        
                        <ListBox x:Name="GoalsListBox" Background="Transparent" 
                               BorderThickness="0" Foreground="{StaticResource TextBrush}">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FF3F3F46"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Right Panel - Chat Interface -->
            <Border Grid.Column="1" Background="{StaticResource SecondaryBrush}" 
                    CornerRadius="8" Padding="15" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Chat Header -->
                    <TextBlock Grid.Row="0" Text="💬 Conversation" FontSize="16" FontWeight="Bold" 
                             Foreground="{StaticResource TextBrush}" Margin="0,0,0,15"/>
                    
                    <!-- Chat Messages -->
                    <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer" 
                                VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                        <StackPanel x:Name="ChatMessagesPanel">
                            <!-- Welcome Message -->
                            <Border Background="#FF2D5A2D" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                                <TextBlock Text="Welcome to MiniBrain Desktop! Direct Claude API mode - just type your message and send!" 
                                         Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <!-- Chat Input -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="ChatInputTextBox" Grid.Column="0"
                               Style="{StaticResource ModernTextBox}"
                               KeyDown="ChatInputTextBox_KeyDown"
                               Margin="0,0,10,0"/>
                        
                        <Button x:Name="SendButton" Grid.Column="1" Content="📤 Send" 
                              Style="{StaticResource ModernButton}"
                              Click="SendButton_Click" IsEnabled="True"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryBrush}" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusTextBlock" Text="Ready" 
                         Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="ApiStatusTextBlock" Text="🔴 Claude API: Disconnected" 
                             Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"
                             Margin="0,0,20,0"/>
                    <TextBlock x:Name="DbStatusTextBlock" Text="🔴 Database: Disconnected" 
                             Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
